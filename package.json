{
  "private": true,
  "name": "worker",
  "version": "1.0.0",
  "description": "Cloudflare Webhook Relay Worker",
  "main": "index.js",
  "scripts": {
    "dev": "npx wrangler@beta dev index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Cloudflare",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
